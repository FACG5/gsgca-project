// const dbConnection = require('../db_connection');
// select (select count(*) from cohort) as count_cohort, (select count(*) from project) as count_project, (select count(*) from student) as count_student

